aliases:
  - 'copy:view'
watch:
  files:
    - '<%= dev %>/index.html'
  tasks:
    - 'view'
inline:
  src: '<%= dist %>/index.html'
copy:
  src: '<%= dev %>/index.html'
  dest: '<%= dist %>/index.html'

